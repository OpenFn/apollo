# behaviour

Specs about what the assistant does with a request, rather than how good the
artifact it produces is: when to act versus answer, when to ask, how much of the
request to take on.

A spec belongs here if it would still be meaningful with the workflow and code
swapped out — the subject is the decision, not the domain.
